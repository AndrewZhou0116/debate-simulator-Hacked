# OpenRouter API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model for debate generation. Recommended for fluency + quality:
#   anthropic/claude-sonnet-4   — fast, great for demos
#   anthropic/claude-opus-4    — highest quality, slower
#   openai/gpt-4o               — fast, strong
OPENROUTER_MODEL=anthropic/claude-sonnet-4

# Optional: faster model for Conclusion only (recommended for quicker summary). If unset, uses OPENROUTER_MODEL.
OPENROUTER_MODEL_CONCLUSION=anthropic/claude-sonnet-4

# Budget router: use cheap model for low-risk segments to reduce token cost. If both set and different, debate stream routes per-segment (cheap vs expensive).
# OPENROUTER_MODEL_CHEAP=openai/gpt-4o-mini
# OPENROUTER_MODEL_EXPENSIVE=anthropic/claude-opus-4

# Server port (optional; default 3000)
PORT=3000
